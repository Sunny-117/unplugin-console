你是一个资深前端基础设施工程师，精通 Vite / Webpack / Rollup / esbuild 插件开发，熟悉 unplugin 生态，以及浏览器与Node.js通信机制（WebSocket / SSE / HTTP）。

请帮我实现一个名为 `unplugin-console` 的跨构建工具插件，目标如下：

## 🎯 核心目标

实现“浏览器控制台日志 → 构建工具开发服务器终端”的实时同步：

- 浏览器中的 console.log / warn / error
- 可以实时输出到开发服务器终端（Node.js）
- 用于提升调试体验，并方便 AI 工具分析日志

---

## 📦 技术要求

### 1. 插件框架
- 基于 unplugin 实现（支持 Vite / Webpack / Rollup / esbuild）
- 导出标准插件：
  - `vite`
  - `webpack`
  - `rollup`
  - `esbuild`

---

### 2. 浏览器端能力

需要自动注入一段 runtime 代码，实现：

- 劫持以下方法：
  - console.log
  - console.info
  - console.warn
  - console.error

- 保留原始行为（仍然输出到浏览器控制台）

- 将日志发送到开发服务器：
  - 使用 WebSocket（优先）
  - fallback: fetch / HTTP

- 发送数据格式：

```json
{
  "type": "log | warn | error | info",
  "args": [...],
  "timestamp": number,
  "source": "browser",
  "stack": string
}
```

### 3. 开发服务器端能力

#### Vite:

- 使用 `configureServer`
- 创建 WebSocket server（或复用 Vite ws）

#### Webpack:

- 使用 devServer setup hooks

#### Rollup / esbuild:

- 提供 Node server（可选）

------

### 4. 终端输出

在 Node 终端打印日志：

- 使用不同颜色区分：
  - log → 默认
  - info → 蓝色
  - warn → 黄色
  - error → 红色
- 输出格式示例：

```bash
[unplugin-console][browser][warn]
message: xxx
stack: xxx
```

------

### 6. 关键实现点

#### A. 注入 runtime

- 通过 transform 或 virtual module 注入
- 自动在入口执行

#### B. WebSocket 通信

- 浏览器自动连接 ws://localhost:xxxx
- 支持断线重连

#### C. 序列化 console 参数

- 支持对象、Error、循环引用
- 安全 stringify

#### D. Source Map 支持（加分项）

- 能够定位日志来源文件位置

------

### 7. 高级功能（请尽量实现）

- 支持过滤日志级别（如只输出 error）
- 支持自定义前缀
- 支持关闭插件
- 支持生产环境自动禁用

------

### 8. 输出要求

请输出：

1. ✅ 完整 TypeScript 实现代码
2. ✅ 每个核心模块说明
3. ✅ Vite 使用示例
4. ✅ Webpack 使用示例
5. ✅ 设计思路说明（为什么这样做）
6. ✅ 可扩展点建议（如未来接入 AI）

------

### ⚠️ 注意事项

- 不要只给思路，一定要给完整可运行代码
- 不要省略 WebSocket 细节
- 不要假设任何外部工具存在
- 代码要尽量简洁但工程化，要有良好的架构设计
- **Vitest 单元测试驱动开发**



基于我现有的代码进行开发